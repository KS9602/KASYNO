spring:
  application:
    name: GameService

  datasource:
    url: jdbc:postgresql://game-db:5432/gs_db
    username: gs_user
    password: gs_pass

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect

